server {
    listen {{ nginx_port }} default_server;
    listen [::]:{{ nginx_port }} default_server;

    root {{ root_dir }};
    index index.html;
    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }
}
